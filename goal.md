90% — core pipeline done: Dart cognitive complexity, fan-in/out, coverage (optional),
dead-code (optional), git history join, all three renders. 31 tests passing. Validated
end-to-end against a real 164-file Flutter app (chess/app, read-only): 0 parse errors,
plausible complexity ranking.

Next: eyeball codecity.html/codemap.html in an actual browser (only ever opened as raw
HTML/grepped so far, never visually inspected) for anything that looks structurally
wrong — building sizes, district grouping, colour ramps.
